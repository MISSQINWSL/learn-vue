// 保存全局的 Vue
let _Vue = null;

// Store 类
class Store {
    constructor(options) {
        
    }
}

// install 方法负责将 Vuex 可以被 Vue.use() 安装
function install(Vue) {

}

// 导出 install 和 Store
export default {
    install,
    Store,
}